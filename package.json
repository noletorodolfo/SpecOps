{
  "name": "specops-ts-tooling",
  "private": true,
  "description": "Node-side tooling SpecOps uses to syntax-check and test generated TypeScript files before review — not a runnable app.",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.6.3"
  }
}
