# OAuth Integration Pattern
- Use Authorization Code flow for server-side apps.
- Store refresh tokens encrypted; rotate keys periodically.
- Healthchecks: token refresh endpoint, error handling and retry strategy.

